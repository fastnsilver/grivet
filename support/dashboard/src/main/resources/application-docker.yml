eureka:
  instance:
    preferIpAddress: true
  client:
    enabled: true
    healthcheck:
      enabled: false
    serviceUrl:
      defaultZone: http://discovery-service:8761/eureka/
    register-with-eureka: false
    
spring:
  redis:
    host: redis
      
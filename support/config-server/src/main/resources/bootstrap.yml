server:
  port: 8888
  compression:
    enabled: true
  undertow:
    accesslog:
      enabled: true

spring:
  application:
    name: @project.artifactId@
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fastnsilver/config-repo.git
          searchPaths: '{application}'

server:
  port: 8888
  compression:
    enabled: true
  undertow:
    accesslog:
      enabled: true

spring:
  application:
    name: grivet-config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fastnsilver/grivet-config-repo.git
          #uri: file://${user.home}/development/projects/personal/grivet-config-repo

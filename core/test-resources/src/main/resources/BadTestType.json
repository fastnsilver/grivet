{
    "type": "PurchaseOrder",
    "description": "This seems like it should be a valid type definition for a purchase order, but Grivet currently lacks direct support for boolean attributes.  Work-around is to use an "int" (0/1) or a "varchar" ("true"/"false").
    "attributes": 
    {
        "vendorId": "varchar",
        "contactPersons": "json",
        "orderNumber": "bigint",
        "referenceNumber": "bigint",
        "createdDate": "iso_date",
        "dueDate": "iso_date",
        "exchangeRate": "decimal",
        "isInclusiveTax": "boolean",
        "vatTreatment": "varchar",
        "lineItems": "json",
        "notes": "text",
        "shipVia": "text",
        "deliveryAddress": "text",
        "attention": "varchar"
    }
}